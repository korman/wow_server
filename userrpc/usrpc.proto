syntax = "proto3";

package usrpc;
option go_package="./usrpc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Usrpc {
  rpc Ping(Request) returns(Response);
}
